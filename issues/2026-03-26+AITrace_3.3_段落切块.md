# AITrace 3.3 段落 + 章节锚点切块（执行记录）

## 上下文

- 依据 `docs/Tech_Spec_AI_Review_4_DOCX_Migration.md` §3.3：AITrace 仅以 Hybrid Parser 的 Markdown 为输入，约 2000 字、段落边界切块，锚点 `--- [章节名 - 第 N 段] ---`。
- 移除 PDF 按页与 `pdf-page-text.ts`，切块逻辑迁至 `lib/review/aitrace-markdown-chunks.ts`；卸载 `pdfjs-dist`。

## 已做

- `buildAiTraceChunksFromMarkdown`：ATX 标题感知、章节内段号、超长单段不中段切断。
- `aitrace.service`：`location_anchor` 替代 `page`；排序按 `location_anchor` + severity。
- `config/prompts.default.json`：`ai_trace_system` 1.1.0。
- `npm test`：`lib/review/aitrace-markdown-chunks.test.ts`。

## 备注

- 生产环境若 Supabase 存了提示词副本，需同步 `ai_trace_system` 模板或 `init-config`。
