# 细化 Trigger 编排层方案

**日期**: 2026-03-21  
**状态**: 已完成

## 上下文
现有的 `Tech_Spec_AI_Review_2_Trigger.md` 过于简略，没有与三层架构（UI展示层、业务逻辑层、数据访问层）以及最新的工作台数据库结构完全对齐。特别是缺失了后台任务如何通过 `review.admin.dal.ts` 回写实时进度 (`stages` JSONB)，以及如何从 Action 层正确触发 Job。

## 计划内容
1.  **模块定位与三层架构映射**：补充 DAL 和 Action 层的具体文件依赖。
2.  **数据访问层 (DAL) 设计 (Admin Only)**：详细定义 `review.admin.dal.ts` 需要暴露的核心接口（如 `updateReviewStages`, `completeReview`, `markAsFailed`），强调必须使用 Service Role 绕过 RLS。
3.  **业务逻辑层 (Server Action) 设计**：定义 `startReviewEngine` 的完整控制流和异常处理，预留扣点逻辑。
4.  **主调度 Job 核心实现**：给出真实可用的 TypeScript 骨架代码，展示如何并发调用 Engine 服务，并在各个 Promise 生命周期中回写 `stages` JSONB 驱动前端 Realtime。
5.  **稳健降级策略 (Fallback)**：完善原有的降级逻辑，补充与 Engine 层的衔接代码。

## 执行结果
已将以上计划内容完整重写至 `docs/Tech_Spec_AI_Review_2_Trigger.md`。