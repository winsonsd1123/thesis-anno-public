# 2026-03-24 参考文献多源核查（Spec 3.5）

## 上下文

按计划实现：`ReviewAnalyzeContext` 增加 `referenceExtract` / `referenceVerify`；`config/prompts.default.json` 增加 `reference_extract` 与 `reference_verification`；`trigger/review-orchestrator.ts` 注入；新增 `lib/integrations/academic/` 瀑布检索；`reference.service.ts` 实现 `extractReferencesFromPDF` 与 `verifyReferenceBatch`。

## 环境变量

- `ACADEMIC_API_CONTACT_EMAIL`（可选，CrossRef/PubMed 礼貌池；未设时回退 `NEXT_PUBLIC_CONTACT_EMAIL` 或占位邮箱）
- `SEMANTIC_SCHOLAR_API_KEY`（可选）
- `OPENROUTER_API_KEY`（已有）

## 验证

- `pnpm exec tsc --noEmit`、`pnpm lint`、`pnpm build` 通过
- `searchSourcesWaterfall` 对标题「Attention Is All You Need」烟测返回 CrossRef 候选

## 备注

若 Supabase Storage 中的 `prompts` 配置未包含新 key，编排会在运行时抛错；需同步上传或沿用默认文件。
