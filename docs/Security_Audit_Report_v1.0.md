# Security Audit Report

**Date:** 2026-03-16
**Auditor:** Colin (AI Assistant)
**Scope:** Codebase Audit (No code modifications)

## 🚨 Critical Risks (High Priority)

### 1. Middleware Cookie Synchronization Issue
*   **Location**: [middleware.ts](file:///Users/wenyujun/dev/project/thesis-annotation/thesis-anno-public/middleware.ts#L45-L52)
*   **Issue**: The middleware creates a Supabase client (which attempts to handle session refreshing via response cookies) and then separately creates an `intlResponse`. While headers are copied, the complex `Set-Cookie` handling in `NextResponse` might be lost or incorrectly overwritten when creating a new response object.
*   **Impact**: User session state might be lost unpredictably, or token refreshing might fail, causing frequent logouts.
*   **Recommendation**: Avoid creating two separate response objects. Perform Auth checks first, and if passed, initialize the Supabase client directly on the `intlResponse`, or ensure cookie operations are performed on the final response object.

## ⚠️ High Risks (Medium Priority)

### 2. Payment Webhook Supports GET Requests
*   **Location**: [app/api/billing/webhook/zpay/route.ts](file:///Users/wenyujun/dev/project/thesis-annotation/thesis-anno-public/app/api/billing/webhook/zpay/route.ts#L65-L72)
*   **Issue**: The webhook handler explicitly supports GET requests for compatibility.
*   **Impact**: Sensitive parameters (Order ID, Amount) in GET requests are exposed in URL logs.
*   **Recommendation**: Strictly enforce `POST` for server-to-server callbacks. Remove the `GET` handler.
*   **Status**: **Accepted Risk (Won't Fix)**. ZPay documentation explicitly states that the notification method is `GET`. We must support `GET` to receive payment confirmations. The risk is mitigated by signature verification (checksum) which prevents tampering.

### 3. Host Header Injection Vulnerability
*   **Location**: [app/api/billing/create-order/route.ts](file:///Users/wenyujun/dev/project/thesis-annotation/thesis-anno-public/app/api/billing/create-order/route.ts#L8-L14)
*   **Issue**: The code trusts the `x-forwarded-host` header to construct the callback URL.
*   **Impact**: If deployed behind a misconfigured proxy, an attacker could spoof the `Host` header to generate payment links with callback URLs pointing to their own server.
*   **Recommendation**: Configure `ALLOWED_HOSTS` or strictly use `NEXT_PUBLIC_SITE_URL` from environment variables instead of relying on dynamic headers.
*   **Status**: **Accepted Risk (Won't Fix)**. Dynamic host resolution is required for correct callback URL generation in multi-environment deployments (especially local dev with tunnels like ngrok). Strict static configuration breaks the payment callback loop due to TLS/SNI compatibility issues with the legacy ZPay server. The risk is minimal as the attacker cannot forge the payment signature.

## ℹ️ Low Risks / Improvements

### 4. Lack of Rate Limiting
*   **Location**: Global API, specifically `app/api/billing/estimate-cost` and `create-order`.
*   **Issue**: No visible rate limiting logic.
*   **Impact**: Vulnerable to resource exhaustion attacks (DoS) via `estimate-cost` or database pollution via `create-order`.
*   **Recommendation**: Implement `upstash/ratelimit` or a simple IP-based rate limiter in Middleware.

### 5. Missing Content Security Policy (CSP)
*   **Location**: `next.config.ts`
*   **Issue**: No CSP headers configured.
*   **Impact**: Increased risk of XSS if a vulnerability is introduced (e.g., via third-party scripts), allowing attackers to load malicious scripts or styles.
*   **Recommendation**: Configure `Content-Security-Policy` headers in `next.config.ts` or Middleware.

### 6. Fragile UUID Conversion Logic
*   **Location**: [lib/dal/order.dal.ts](file:///Users/wenyujun/dev/project/thesis-annotation/thesis-anno-public/lib/dal/order.dal.ts#L53-L56)
*   **Issue**: Hardcoded regex replacement for converting 32-char strings to UUIDs.
*   **Impact**: If the input format changes slightly or doesn't match the expectation, the conversion might fail or produce invalid UUIDs.
*   **Recommendation**: Store the raw 32-char ID or use a robust UUID parsing library/method that handles various formats.

## ✅ Positive Findings

*   **Server Actions Validation**: `auth.actions.ts` correctly uses Zod for input validation and handles errors gracefully without exposing stack traces.
*   **Atomic Operations**: `transaction.service.ts` and `wallet.dal.ts` use database RPC functions to ensure data integrity during balance updates.
*   **Payment Verification**: The webhook correctly verifies signatures, amounts, and order status (idempotency).
*   **Privilege Separation**: Clear separation between `createAdminClient` (Service Role) and standard RLS-enabled clients.

---
*Report generated by Trae AI Assistant on 2026-03-16*
